name: CI/CD - Flask Calculator

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout del código
        uses: actions/checkout@v3

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🐳 Login en DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🔧 Construir y etiquetar imagen
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/flask-calculator .
          docker tag ${{ secrets.DOCKERHUB_USERNAME }}/flask-calculator ${{ secrets.DOCKERHUB_USERNAME }}/flask-calculator:latest

      - name: 📤 Subir imagen a DockerHub
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/flask-calculator

      - name: 🚀 Desplegar manualmente en Railway (100% limpio con jq)
        env:
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          echo '{}' | jq --arg projectId "$RAILWAY_PROJECT_ID" \
            --arg query 'mutation Deploy($projectId: String!) { deploymentCreate(input: {projectId: $projectId}) { id } }' \
            '.query = $query | .variables.projectId = $projectId' > payload.json

          curl -X POST https://backboard.railway.app/graphql \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $RAILWAY_TOKEN" \
            --data-binary @payload.json
